import React from 'react';

interface MoviesFeedProps {
    
}

const MoviesFeed: React.FC<MoviesFeedProps> = () => {
    return (
        <div>
            movies feed
        </div>
    );
};

export default MoviesFeed;